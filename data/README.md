# Data

Aucun contenu nuisible n'est hébergé dans ce dossier. Les seeds sont chargées
à l'exécution depuis des benchmarks publics externes (walledai/AdvBench,
tatsu-lab/alpaca) via `argos/data.py`. Les fichiers générés localement sont
ignorés par git (voir `.gitignore`).
