# Data

Ce dossier n'héberge aucun contenu nuisible. Les seeds sont chargées à
l'exécution depuis des benchmarks publics externes (ex. AdvBench, HarmBench)
via `argos/data.py`. Les fichiers générés localement sont ignorés par git
(voir `.gitignore`).
